---
/* Shop retired 2026-07-25 (Neil: "remove the shop section"). A 301 rather than a delete so
   any existing link or index entry lands on the closest live page instead of a 404. */
export const prerender = false;
return Astro.redirect('/free', 301);
---
