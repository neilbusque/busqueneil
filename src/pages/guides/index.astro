---
// The tutorials hub is /resources now. The individual /guides/* URLs stay exactly where they
// are so their rankings are untouched; only this thin index (25 words) folds into the hub.
export const prerender = false;
return Astro.redirect('/resources', 301);
---
